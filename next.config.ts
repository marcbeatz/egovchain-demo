/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // basePath: '/egovchain-demo',
  // trailingSlash: true,
};

module.exports = nextConfig;
